import 'package:flutter/material.dart';

class BreakingNewsItems extends StatelessWidget {
  const BreakingNewsItems({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}